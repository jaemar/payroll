%nav.top-bar{"data-topbar" => "", :role => "navigation"}
  %ul.title-area
    %li.name
      %h1
        = link_to "Payroll", "#"
    %li.toggle-topbar.menu-icon
      %a{:href => "#"}
        %span Menu
  %section.top-bar-section
    %ul.right
      %li
        - if user_signed_in?
          = link_to "Logout", destroy_user_session_path, :method => :delete
        - else
          = link_to "Log in to My Account", "#"
